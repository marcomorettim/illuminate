// GENERATED at assemble time (Stage 5) from build-manifest.json + node-*.md.
// This checked-in stub keeps the empty template compiling; scripts/build.mjs overwrites it.
import { Manifest } from './types';

export const manifest: Manifest = {
  meta: { source: 'empty.docx', governing_thought: 'Empty template — no source assembled yet.', title: 'illuminate', kicker: 'template' },
  nodes: {},
  roots: [],
};

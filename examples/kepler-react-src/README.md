# Kepler — React reference build (illuminate, modern stack)

`/illuminate` on a 21.7k-word single-thesis strategy article (six domains, one flywheel), authored on
the modern stack — React + TS + Tailwind + Radix (shadcn) + TanStack Table + visx + Framer Motion —
bundled to one offline HTML via `vite build` + `vite-plugin-singlefile`. Output: `../illuminate-kepler-flywheel.html`.

```bash
npm install && npm run build
node ../../render-gate/render-gate.mjs dist/index.html
```

Every domain→driver→mechanism is a uniform data model (Completeness Law §1.1a); moat identity
M = D×B×L×T; the flywheel hub-and-spoke graph, sequencing table, and scenario contribution track are
visx/Radix; nested accordion drill-down + master-detail evidence Sheet; One-Dark code; Beitar palette
with the fixed-dark finding foreground. `src/fonts.css` = `assets/embedded-font.css`.
